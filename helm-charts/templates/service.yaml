apiVersion: v1
kind: Service
metadata:
  name: {{ include "openapi-server-spring.fullname" . }}
  labels:
    {{- include "openapi-server-spring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openapi-server-spring.selectorLabels" . | nindent 4 }}
