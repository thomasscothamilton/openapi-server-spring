apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openapi-server-spring.fullname" . }}-test-connection"
  labels:
    {{- include "openapi-server-spring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openapi-server-spring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
